import { en } from "./en";
import { es } from "./es";
import { fr } from "./fr";
import { it } from "./it";
import { de } from "./de";

export const translations = {
  ...en,
  ...es,
  ...fr,
  ...it,
  ...de
};

export default {
  translations
};
